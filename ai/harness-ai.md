Learning Ai Harness - Make ai agent working in closed loop until works get done 
1. What new agents are failing at early victory, no traceability, verify work manage context.

Why Capable harness still fails.
The reason is very clear that like the througbred need saddle for better running experiece just like a better harness need to write and maintain a large repo code instead of going to expensive models or premium one.
If agent fail to produce a better output at code check the harness intead of going expensive models. For example a simple Agents.md files helps in generating a better code by simply adding what coding structure is currently being used and how to verify the changes.

Before going 
1. Task is specified correctly
2. Enought Context is present.
3. Execution environment.
4. Verification feedback.
5. State are managed.

Design -> Code -> Review -> Test


What harness actuall is.
1. Everything inside engineering infrastructure out the model weights.
Lets take an analog where ai models treated as engineer who joined the team and instantly called him/her to
work on task where he does not have documentation of the code and well commented code.


Make the Repository single source of truth.
Ideal. A repo is spec for ai agent 
Info that does not exist in the repo does not exist for the agent.
Cold start test.
Every repo must contain.
1. What this Project is.
2. How this project is organized.
3. How to run this project. 
4. How do i verify this project.
5. what to do next. 

What new learn Context Anxiety?
When claude or anthropic model sees that context window is low its try to wrap up things without verification so that the use of context is low or never reached beyond that.
